import {createReducer} from "@reduxjs/toolkit";


const initialState = {
    c :0,
};

export const  customReducer = (initialState, {
    // in this we can pass multile reducer
    // this is the action that will call when the action is dispatched
    // here we have to created reducer
})